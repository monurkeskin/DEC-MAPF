# Analysis from explicit evidence

`experiment.py` preserves all planned denominators, matches treatments by exact identities, computes common-solved costs and resamples complete scenario blocks. JSON/CSV/LaTeX/SVG/PDF/PNG use one table and cohort receipt. `post_simulation.py` reads checked telemetry chunks; missing utilities and incomplete token rosters remain explicit. Historical aggregate imports stay quarantined.

See the [current contract](../../../docs/EXPERIMENTS.md), the source interfaces and executable tests.
